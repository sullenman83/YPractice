apiVersion: 1

providers:
  - name: 'EventAPI Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      # Путь внутри контейнера к папке с вашими JSON-файлами
      path: /etc/grafana/provisioning/dashboards/json_files